# betterxeneon-presets (Phase 3 placeholder)

The preset rail UI — left-hand strip of saved layouts the user taps to switch the canvas. Likely a sub-component of the loader rather than a standalone `.icuewidget`. Decision deferred until the loader is being designed.
